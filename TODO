
stats_file.c
  Make program version line a comment for easier parsing.
  Put btime in header, remove from ps.
  ! type schema
  @ type devices
  # comment
  $ reserved
  % type prop rest
  Allow space after directive character.

stats.c
  Fix bsearch().
  Property lists.
  val_t.

ib
  Use "hca/port" for device name.

  master.ls4:jhammond$ sudo perfquery -x
  ibwarn: [3918] dump_perfcounters: PerfMgt ClassPortInfo 0x400 extended counters not indicated

  # Port extended counters: Lid 1466 port 1
  PortSelect:......................1
  CounterSelect:...................0x0400
  PortXmitData:....................37880598370
  PortRcvData:.....................832555876
  PortXmitPkts:....................80434330
  PortRcvPkts:.....................44644020
  PortUnicastXmitPkts:.............0
  PortUnicastRcvPkts:..............0
  PortMulticastXmitPkts:...........0
  PortMulticastRcvPkts:............0

mem
  Separate meminfo and numastat.

intel_uncore
  Use physical package id as device.  Topology.

vm
  Remove, convert to swap.

block
  Easy way to ignore inactive devices?

TODO swap
  Check /proc/swaps  

lustre
  mdc
  MDS/{mds,mds_readpage}.
  OSS/{ost,ost_io,ost_create}.
  More keys.

shmem
  Check /proc/mounts 'tmpfs /dev/shm tmpfs ...'  On older kernels
  first column is 'none.'
  /proc/sysvipc/shm 'key shmid perms size ...'  Size units?


Trying to collect unknown type creates empty record.  NBD.

More /sys, less /proc.

Skip dirents with d_name[0] == '.'.

Track which stats/values were actually set.

Replace asnprintf() with snprintf().

Enabling, disabling types.

Checking for whitespace, other garbage, in ids.

Torture test memory management.

Torture test dictionary.
dict_resize()
dict_allow_resize()

Output file, directory naming.

MAYBE
  /proc/net/rpc/nfs
  /proc/net/rpc/nfsd
  /proc/buddyinfo
  /proc/schedstat (Mostly unavailable.)
  /proc/slabinfo
  /proc/net/sockstat[6]
  /proc/net/snmp
