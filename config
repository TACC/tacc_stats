TYPES += amd64_pmc
TYPES += block
TYPES += cpu
# ib
# ib_ext
# intel_pmc3
# intel_uncore
TYPES += llite
TYPES += lnet
# lustre
TYPES += mem
TYPES += net
TYPES += ps
TYPES += sysv_shm
TYPES += tmpfs
TYPES += vfs
TYPES += vm
